# Screenshots

This directory holds the screenshots referenced by the main [`README.md`](../../README.md). None have been captured yet — add the following two PNGs here, using these exact filenames so the main README picks them up automatically:

| Filename | What to capture |
|---|---|
| `hero.png` | The Home Interface — the hero / welcome screen on first load, before any question is asked |
| `chat-demo.png` | The Chatbot in Action — a real question-and-answer exchange in the chat interface |
