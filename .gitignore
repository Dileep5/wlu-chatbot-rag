# Python
__pycache__/
*.pyc

# VS Code
.vscode/

# Claude Code local settings (already excluded from Docker builds too -
# see .dockerignore)
.claude/

# Mac
.DS_Store

# ChromaDB generated files
data/vector_db/

# Refresh pipeline logs (timestamped, regenerated every run - see
# .github/workflows/weekly-refresh.yml, which uploads them as a workflow
# artifact instead of committing them)
logs/
*.log

# pytest
.pytest_cache/

# Python virtual environments
venv/
.venv/
.env